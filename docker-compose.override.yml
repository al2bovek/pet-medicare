 services:

  server:
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    ports:
      - "3000:5000"
    volumes:
      - ./server:/app
      - /app/node_modules
    command: npm run dev


  pgadmin:
    ports:
      - "5050:80"

      